```
在一种方法中，数字使用被称为“帕斯卡三角”的方案进行加密，当一个数字数组被输入到这个系统时，它会将相邻的数字相加。然后它将每次加法最右边的数字（最低有效数字）用于下一步。因此，每一步的位数都减一，重复这个过程，直到只剩下2位数。这个2位数的序列就形成了加密的数字。

给定数字的初始序列，找到加密的数字。您应该报告一串代表加密数字的数字。

例子
数字 = [4,5,6,7]
加密发生如下：
4 5 6 7
 9 1 3
  0 4
因此，形成的加密数字是04
```

```js
// 模拟即可
// 类似数据结构中手动构造霍夫曼树的过程
```

